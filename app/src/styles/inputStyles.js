import { StyleSheet } from 'react-native'

export const inputStyles = StyleSheet.create({
  text: {
    height: 40,
    margin: 12,
    borderWidth: 1,
    padding: 10,
    color: '#000',
    backgroundColor: '#fff'
  }
})
